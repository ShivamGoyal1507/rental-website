<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirm Delete</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheet/style.css">
</head>
<body class="min-h-screen flex items-center justify-center bg-gray-100">

    <div class="bg-white p-6 rounded-lg shadow-md w-full max-w-md text-center">
        <% if (user.image) { %>
            <img src="/images/<%= user.image %>" alt="Profile" class="w-24 h-24 rounded-full object-cover mx-auto mb-4">
        <% } else { %>
            <div class="w-24 h-24 bg-gray-300 rounded-full flex items-center justify-center mx-auto mb-4 text-gray-600">
                No Image
            </div>
        <% } %>

        <h2 class="text-xl font-semibold text-gray-800 mb-1"><%= user.name %></h2>
        <p class="text-sm text-gray-600">Room Number: <%= user.room %></p>
        <p class="text-sm text-gray-600">Family Members: <%= user.family %></p>
        <p class="text-sm text-gray-600 mb-4">
            Joining Date: 
            <% if (user.doj) { %>
                <%= user.doj.toDateString() %>
            <% } else { %>
                Not Available
            <% } %>
        </p>

        <p class="text-red-600 font-medium mb-4">Are you sure you want to delete this user?</p>

        <div class="flex justify-center gap-4">
            <a href="/delete/<%= user._id %>" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded">
                Yes, Delete
            </a>
            <a href="/edit_user/<%= user._id %>" class="bg-gray-300 hover:bg-gray-400 text-black px-4 py-2 rounded">
                Cancel
            </a>
        </div>
    </div>

</body>
</html>